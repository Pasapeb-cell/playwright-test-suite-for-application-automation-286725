{
  "name": "playwright-test-suite-for-application-automation-286725",
  "version": "1.0.0",
  "private": true,
  "description": "Automated Playwright end-to-end test suite designed to execute browser-based tests against the Kanban task management application or similar web apps.",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "preinstall": "node -e \"process.exit(0)\"",
    "postinstall": "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true playwright install --with-deps || PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true npx --yes playwright@1.57.0 install --with-deps",
    "prepare": "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true playwright install --with-deps || PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true npx --yes playwright@1.57.0 install --with-deps",
    "init": "npm ci || npm install && PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true playwright install --with-deps || PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true npx --yes playwright@1.57.0 install --with-deps",
    "build": "echo 'No build step required for Playwright tests'",
    "test": "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true playwright test",
    "test:headed": "PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1 CI=true playwright test --headed",
    "test:report": "playwright show-report",
    "test:dryrun": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "playwright": "1.57.0"
  }
}
